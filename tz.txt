Корректировки в файл необходимые

1. Сейчас мы создаём popup всплывающий при нажатии, и его размеры указываются в коде, но мы всё равно создаём грубо говоря этот popup в создаваемом окне, которое я не понимаю где и как регулировать, оно не указано в коде, нужно найти, мне нужно увеличить именно исходные размеры в принципе создаваемого расширения при нажатии на клавишу, тщательно нужно проверить это и учитывать в коде


2. Нужно добавить ещё один поиск, на этот раз по csv файлу Nokia_Check_sync_BS_and_Verification_parameters мы в нём по столбцу С значение наше, которое мы пишем в поиске и рядом с создаваемыми столбцами, которые мы уже создаём создать ещё одно поле, под именем Синхронизация, куда вставлять значения ячейки N O P Q R которое именовать в таблице соответственно Router, PtpLoopBack, Port, Vlan, GW.


Когда мы увеличим в ширину вот то самое ИСХОДНОЕ окно расширения, то все наши выводы корректно будут смотреться на одном экране

---
Выполненные корректировки:

1) Увеличен именно исходный размер окна расширения (не только контент внутри popup):
- Из `manifest.json` удалён `action.default_popup`, добавлен `background.service_worker` и permission `windows`.
- Добавлен файл `background.js`, который по клику на иконку расширения открывает отдельное popup-окно Chrome через `chrome.windows.create(...)` с размерами 1880x900.
- Файлы: `manifest.json`, `background.js`.

2) Обновлена верстка popup под увеличенное исходное окно:
- В `popup.html` увеличены базовые размеры body (шире/выше), чтобы данные корректно умещались на экране в новом окне.
- Лейаут расширен с 3 до 4 колонок, добавлена новая колонка "Синхронизация" с контейнером `syncContainer`.
- Файл: `popup.html`.

3) Добавлен новый поиск по CSV `Nokia_Check_sync_BS_and_Verification_parameters.csv`:
- В `popup.js` добавлена загрузка файла (`loadSyncCsv`) при фоновой загрузке BUILD-данных.
- Добавлен парсер `parseSyncCsv`:
  - поиск по колонке C (код БС),
  - чтение значений колонок N/O/P/Q/R.
- Добавлен рендер таблицы `renderSyncResults` в новую колонку "Синхронизация".
- Колонки в выводе названы по ТЗ: `Router`, `PtpLoopBack`, `Port`, `Vlan`, `GW`.
- Новый блок подключён в общий Build-вывод и в очистку результатов.
- Файл: `popup.js`.

4) Дополнительно исправлены технические мелочи в `popup.js`:
- Удалено дублирование `table.appendChild(tbody)` в таблице 2G.
- Удалено дублирующее объявление `const td` в таблице ANT+RMOD.
