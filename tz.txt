Ещё корректировки:

1. сейчас я получаю вот такие данные, НАПРИМЕР вот я получаю тег и значения через |


LANIF_TNLSVC	1 | 1 | 1 | 1 | 1
VLANIF_TNL	1 | 1 | 1 | 1 | 1
VLANIF_ETHSVC	1 | 1 | 1 | 1 | 1
VLANIF_ETHIF	1 | 1 | 1 | 1 | 1
VLANIF_VLANIF	1 | 2 | 3 | 4 | 9
VLANIF_vlanid	200 | 400 | 700 | 300 | 4004
VLANIF_userLabel	abis | s1 | lte_oam | iub | Sync

Как ты видишь совпадений 5 и 
Их давай объединим в один столбец в выводе под именем VLAN где распишем их более компактно и объединим в одну ячейку


А эти теги, вот для примера теги и их случайные значения

BBMOD_R_EQM_R	1 | 1 | 1 | 1
BBMOD_R_APEQM_R	1 | 1 | 1 | 1
BBMOD_R_CABINET_R	1 | 1 | 1 | 1
BBMOD_R_BBMOD_R	1 | 2 | 3 | 4
BBMOD_R_configDN	MRBTS-610614/EQM-1/APEQM-1/CABINET-1/BBMOD-4 | MRBTS-610614/EQM-1/APEQM-1/CABINET-1/BBMOD-3 | MRBTS-610614/EQM-1/APEQM-1/CABINET-1/BBMOD-2 | MRBTS-610614/EQM-1/...
BBMOD_R_productCode	473096A.103 | 473096A.103 | 473096A.104 | 473096A.105
BBMOD_R_productName	ABIA AirScale Capacity | ABIA AirScale Capacity | ABIA AirScale Capacity | ABIA AirScale Capacity
BBMOD_R_serialNumber	L1173303514 | K9181503907 | DH201997771 | DH203890175
BBMOD_R_verticalPosition	2.0 | 4.0 | 3.0 | 2.0

Выдавать тоже группировкой, то есть брать 1 значение из всех этих тегов, то есть брать первое значение первой строки, первое значение второй строки и так далее до первго значения последней из этих ячеек и делать общий столбец который назовём RMOD1 далее второе значение первой строки, второе значение второй строки и это RMOD2

2. Так же у меня окно приходится скроллить, данные должны быть выведены на один экран без необходимости скроллить наше главное окно приложения, проверь чтобы оно было побольше и данные помещались на всём экране

3. Проверь вывод в нашем разделе LNMME_accMmePlmnsList если мы получаем в ячейке MCC=250 то красить в зеленый ячейку. 

4. НЕ РАБОТАЕТ ИСТОРИЯ. у нас есть нажимные клавиши с теми обектами которые мы искали ранее, но при нажатии на них мы не возвращаемся к ним почему-то, а должны, допустим я поискал 610614 потом поискал 231216 и потом я смогу нажать в нашем разделе история на 610614 и у меня снова загрузится значения по этому объекту. ТАК ЖЕ ВАЖНО ЧТОБЫ ИСТОРИЯ СБРАСЫВАЛАСЬ ПРИ ЗАКРЫТИИ ПРИЛОЖЕНИЯ

Дополни этот файл информацией об изменениях для дальнейшего использования и всегда в дальнейшем обновляй его






---

Обновление от агента (выполнено):

1) VLANIF группировка реализована:
- Вместо отдельных одинаковых тегов выводится единый столбец `VLAN`.
- Значения с разделителем `|` собираются в компактные блоки вида `VLAN1`, `VLAN2`, ... в одной ячейке.

2) BBMOD_R группировка реализована:
- Добавлена логика формирования карточек `RMOD1`, `RMOD2`, ...
- Для каждого RMOD берется N-е значение из каждого BBMOD_R тега (EQM_R, APEQM_R, CABINET_R, BBMOD_R, configDN, productCode, productName, serialNumber, verticalPosition).

3) Окно расширено и переразмечено:
- Увеличены размеры popup до 1280x880.
- Основной контейнер сделан flex-layout, чтобы рабочая область максимально помещалась в видимую область.
- Скролл остается только внутри блока результатов, а не у всего окна.

4) LNMME_accMmePlmnsList подсветка MCC=250:
- Проверка для `accMmePlmnsList` сохранена и усилена регулярными выражениями.
- При обнаружении MCC=250 ячейка подсвечивается зеленым.

5) История исправлена:
- Клики по истории теперь работают через делегированный обработчик и корректно запускают повторный поиск.
- История хранится только в памяти текущего открытия popup (без localStorage).
- При закрытии popup история автоматически очищается.

6) Дополнительно:
- Обновлен экспорт в буфер (Копировать всё): добавлен формат `RMOD1..N` для BBMOD_R и компактный `VLAN` для VLANIF.


Обновление от агента (выполнено — этап 2):

7) Переименование тегов перед показом пользователю исправлено:
- Добавлена нормализация имени колонки (поддержка префиксов вида `LNMME_...` / `BBMOD_R_...`).
- Теперь переименования из `SHEETS_CONFIG.columns` применяются стабильно даже для «полных» тегов.
- Пример: `LNMME_LNBTS` можно показывать как `ИМЯ БС`.

8) Полноэкранный режим popup:
- Размер интерфейса переведен на `100vw` x `100vh`.
- Содержимое растягивается на весь экран popup, прокрутка ограничена областью результатов.

9) Значения с разделителем `|` теперь отображаются списком:
- Для всех полей при наличии нескольких значений рендерится маркированный список (`ul/li`) вместо одной строки с `|`.
- Это повышает читаемость значений внутри одного объекта.

10) Визуализация `BBMOD_R_verticalPosition`:
- Реализован специальный вид в форме прямоугольника из 4 равных горизонтальных сегментов (`1.0`, `2.0`, `3.0`, `4.0`).
- Сегменты, значения которых присутствуют в данных, подсвечиваются.

